# Hostelxpense_javaproject
A console-based Java app that helps hostel roommates split shared expenses fairly. Log a bill (food, wifi, electricity), auto-split it among participants, track personal budgets with alerts, and view simple spend analytics — all with plain CSV file storage, no database or frameworks required.
